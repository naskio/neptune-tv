/** Default page size for group/channel infinite scroll. */
export const PAGE_SIZE = 100;

/** Sidebar / routing — not real DB group titles. Group detail uses `playerStore` for channel lists. */
export const VIRTUAL_FAVORITE_CHANNELS = "__favorites__";
export const VIRTUAL_RECENTLY_WATCHED = "__recently_watched__";

export const SEARCH_DEBOUNCE_MS = 150;

export const GLOBAL_SEARCH_GROUP_LIMIT = 5;
export const GLOBAL_SEARCH_CHANNEL_LIMIT = 20;
